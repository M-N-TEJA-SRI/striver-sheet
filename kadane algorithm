#include <iostream>
using namespace std;
int main() {
   int n;
   cin>>n;
   int a[n],i,sum=0,maxi=0;
   for(i=0;i<n;i++){
       cin>>a[i];
     sum+=a[i];
     maxi=max(maxi,a[i]);
     if(sum<0)
     sum=0;
        }
cout<<maxi;          
return 0;
}
output
5
9 -9 1 1 2
9
